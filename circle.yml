machine:
  environment:
    NODE_ENV: test
  node:
    version: v8

test:
  override:
    - npm run test-cov
    - nvm use 8 && npm run test-cov
    - nvm use 6 && npm run test-cov
    - nvm use 4 && npm run test-cov
